# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.run

# Environment variables
.env
.env.local
.env.*.local

# Testing
coverage
.nyc_output

# Build artifacts
build

Release-Template.md

# Snyk Security Extension - AI Rules (auto-generated)
.github/instructions/snyk_rules.instructions.md
# Raw vendor icon source drops (regenerate public/icons via scripts/prepare-icons.ts).
AWS-Icons/
NEW-ICONS
Future-Apps.md
# The generated cloud icon sets (public/icons/{aws,azure,gcp}/ + manifests) are
# committed so they deploy with the build — the icon picker fetches them at
# runtime. Used solely for diagrams per each provider's permitted-use guidelines.
